import 'package:flutter/material.dart';

class HoteAndNewMain extends StatelessWidget {
  const HoteAndNewMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("hot and new",
       ),
      ),
    );
  }
}