
import 'package:flutter/material.dart';

class FastLaughsMain extends StatelessWidget {
  const FastLaughsMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("FastLaughs",
     )),
    );
  }
}